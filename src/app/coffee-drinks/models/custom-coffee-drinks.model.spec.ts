import { CustomCoffeeDrinks } from './custom-coffee-drinks.model';

describe('CustomCoffeeDrinks', () => {
  it('should create an instance', () => {
    expect(new CustomCoffeeDrinks( ).toBeTruthy();
  });
});
